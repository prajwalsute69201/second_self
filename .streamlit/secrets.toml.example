# Copy this file's contents into the Streamlit Cloud "Secrets" panel
# (App settings → Secrets), or save as .streamlit/secrets.toml for local use.
# Never commit secrets.toml itself — it is listed in .gitignore.

GROQ_API_KEY = "gsk_xxxxxxxxxxxxxxxxxxxx"
